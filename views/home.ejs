<%- include('partials/header'); -%>
    <h1>Home</h1>
    <p><%=homeStartingContent %></p>
    <% if(posts.length!==0){ %>
    <% posts.forEach((post)=>{ %>
        <h1><%=post.title %></h1>
        <p><%=post.post.substring(0, 100)%>...
        <a href='/posts/<%=post._id %>'>Read More</a></p> 
        
        <% }) }%>
<%- include('partials/footer'); -%>
